# 基于diffusion的汗腺分割方法

## 原理

将三维体数据看作是空间维度的图像序列。

1. 空间连续性： 自回归训练迫使模型理解切片之间的解剖结构联系。例如，预测下一张切片需要模型知道血管、器官是如何在三维空间中延伸的
2. 特征预训练： 通过自回归预训练得到的编码器，已经捕捉到了该体数据的几何特征和纹理规律，这对于下游任务来说，是一个极强的特征初始化

## 方案设计

### 一、图像编码

目标：图像 $x$ 通过编码器 *Encoder* 编码为符合标准正态分布的潜向量 $z$ 且能通过解码器 *Decoder* 解码回图像 $x$

- 数据准备：将三维体数据分别沿着 x-z 和 y-z 面拆解为大量2D切片 $x$
- 推理路径：$x$ -> 🔥*Encoder* -> $z$ -> 🔥*Decoder* -> $x'$
- 损失计算：
  - 重建损失：$x$ 与 $x'$ 的差异(L1、LPIPS)
  - 判别损失：使用 *PatchGAN* 判别器来判断图像的真实性，迫使 *Encoder* 和 *Decoder* 生成更逼真的图像
  - 正则化损失：KL 散度（保证 $z$ 的分布符合标准正态，方便后续采样）

### 二、序列预测

目标：学习 $Z$ 空间中切片之间的变化规律，基于前 $t$ 张切片的潜向量序列 $z_{0:t-1}$ 预测 $z'_t$

- 数据准备：$x_{0:t-1}$ -> ❄️*Encoder* -> $z_{0:t-1}$

#### 训练方案A（自回归）

  - 推理路径：$z_{0:t-1}$ -> 🔥*SeqModel* -> $z'_t$ -> ❄️*Decoder* -> $x'_t$
  - 损失计算：
    - $Z$ 损失：$z_t$ 与 $z'_t$ 的差异
    - 像素损失：$x_t$ 与 $x'_t$ 的差异(L1、LPIPS)

> #### 训练方案B（潜在扩散）
> 
>   - 加噪路径：$z_t$ -> 根据扩散步数 $T$ 加入随机噪声 $\epsilon$ -> $z_t^T$
>   - 去噪路径：将 $z_{0:t-1}$ 作为上下文条件输入模型，从正态分布噪声 $z_t^T$ 中还原出 $z_t$
>   - 损失计算：
>     - 噪声损失：计算模型预测的噪声 ${\epsilon}'$ 与实际加入的噪声 $\epsilon$ 之间的 MSE
>     - 重建损失：计算还原回像素空间后的数值差异

- 序列预测不仅可以是最后一张，也可以是中间一张。不仅有“过去”的记忆，还有“未来”的预判，对于 3D 体数据分割来说，精度通常比只看过去要高。
- 序列长度 $t$ 是一个重要的超参数，有以下几个考虑点：
  1. 感受野与空间常识：太小的话模型只能看到极局部的变化，信息不足，没学会解剖结构。
  2. 计算复杂度：自注意力机制复杂度通常是 O(k^2)，序列越长，显存占用呈平方级增长。对于三维体数据，这通常是限制 $t$ 取值的最大瓶颈。
  3. 任务难度：如果 $t$ 太长，历史信息中的冗余可能干扰当前预测。 $t$ 覆盖目标物体纵深尺寸的1.5倍到2倍是比较理想的平衡点。

### 三、分割任务

#### 方案 A：冻结 *Encoder*

- 做法：在 *Encoder* 后接入一个 U-Net 风格的 *SegDecoder*
- 推理路径：$x$ -> ❄️*Encoder* -> $z$ -> 🔥*SegDecoder* -> $mask$
- 优点：训练量极小，*Encoder* 已经学会了如何识别图像中的纹理和结构，仅训练 *SegDecoder*
- 缺点：没有利用第二阶段 *SeqModel* 的上下文提取能力，鲁棒性差

#### 方案 B：冻结 *Encoder* 和 *SeqModel*

- 做法：在 *Encoder* 和 *SeqModel* 后接入一个 U-Net 风格的 *SegDecoder*
- 推理路径：
  1. $x_{0:t-1}$ -> ❄️*Encoder* -> $z_{0:t-1}$ -> ❄️*SeqModel(去掉最后一层线性层)* -> $d_t$
  2. $x_t$ -> ❄️*Encoder* -> $z_t$
  3. concat($d_t$, $z_t$) -> 🔥*SegDecoder* -> $mask_t$
- 优点：
  - $z_t$ 包含当前切片图像纹理，$d_t$ 包含与前面 $t$ 张图像的的空间结构联系，具备3D空间感
  - 第二阶段已经通过大量的无标签数据学会了3D结构逻辑，分割任务只需要极少量的标注样本就能快速收敛
  - 利用前后切片的上下文信息进行逻辑补偿，对存在图像伪影的切片具有更好的鲁棒性，输出更稳定的分割结果
- 缺点：
  - 计算开销增大
  - 对序列长度 t 敏感，设置得不合理反而会让上下文信息变成干扰噪声，导致分割效果反而不如方案A
  - 潜空间分辨率较低（1/8 或 1/16），分割边缘可能不够精细，对微小目标的捕捉能力受限
  - 为了利用 *Encoder* 前几层特征提升分割精度（U-Net的跳跃连接），必须将 $d_t$ 与 $z_t$ 进行多尺度融合

#### 语义-几何双流融合

- 目的：解决 *SeqModel* 懂逻辑但看不清边界，*VAE* 懂细节但认不出全局的问题。
- 融合方法：
  - 简单拼接：在通道维度拼接后通过卷积层进行信息融合
  - 交叉注意力：以 $z$ 为 Q，以 $d$ 为 K 和 V
  - 门控线性单元：计算权重图: $w=\sigma(Conv(d_t))$，融合后的特征: $f = wz_t + (1- w)d_t$，避免 *SeqModel* 的低分辨率噪声干扰 *Encoder* 的高精度边缘

#### 推理链路

假设：序列长度 $t$ = 4，*Encoder* 下采样倍率 $k$ = 8

1. 输入5张连续切片，*Encoder* 分别输出前4张切片的高维特征图 $(z_0^{1/8}, ..., z_3^{1/8})$ 和第5张切片的多尺度特征图 $z_4^{1/8}、z_4^{1/4}、z_4^{1/2}$
2. 输入 $(z_0^{1/8}, ..., z_3^{1/8})$，*SeqModel* 输出 $d_4^{1/8}$
3. *SegDecoder* 融合 $z_4^{1/8}$ 和 $d_4^{1/8}$ ，上采样后与 $z_4^{1/4}$ 融合，以此类推，输出最终分割掩码

## 环境配置

- torch
- torchvision
- lightning
- einops
- omegaconf
- requests
- albumentations

## 数据集准备

### 数据集结构

```
data_root
├── raw/
│   ├── volume001/
│   │   ├── volume001_0000.png
│   │   ├── volume001_0001.png
│   │   └── ...
│   ├── volume002/
│   └── ...
├── dataset/
│   ├── volume001_xz00x00/
│   │   ├── 0000.png
│   │   ├── 0001.png
│   │   └── ...
│   ├── volume001_xz00x01/
│   ├── volume001_xz01x00/
│   ├── volume001_xz01x01/
│   ├── volume001_yz00x00/
│   ├── volume002_xz00x00/
|   └── ...
└── filelist.txt
```

### 滑窗策略

假设每个体数据由1600张1800×500 的B-scan图像组成，即`x=1800, y=1600, z=500, win_size=256, stride=128`

- 窗口数 `win_num = round((length - win_size) / stride) + 1`

#### x-z面切片

- 沿x和z方向滑窗
- x方向: 13个窗口
- z方向: 2个窗口
- 生成 26 个子体数据，每个子体数据包含 1600 张 256×256 图像，总计 41600 张

#### y-z面切分

- 沿y和z方向滑窗
- y方向: 11个窗口
- z方向: 2个窗口
- 生成 22 个子体数据，每个子体数据包含 1800 张 256×256 图像，总计 39600 张

**单个体数据总计: 81200张 256×256 图像**

## 开发日志

- `2026.3.12`: 添加taming-transformers库，适配lightning==2.x
- `2026.4.10`: fingerprint-scan数据类实现
- `2026.8.11`: 优化fingerprint数据类实现(适配UESTC数据集)，跑通AutoencoderKL训练
- `TODO`: 优化判别器训练稳定性
  - 关闭 BN2d
  - 加上 SpectralNorm 谱归一化
  - 开启双边 label smoothing
  - 判别器损失替换为 relativistic hinge loss（R3GAN）